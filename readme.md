# Mini CRM — Pure PHP Project (PDO + PostgreSQL + Docker)

A small but production-oriented CRM project built with **pure PHP (no framework)**.  
The main goal of this project is to deeply understand backend fundamentals: architecture, SQL, security, and performance problems such as **N+1**.

---

## 🧩 Tech Stack
- PHP 8.x (strict types)
- PostgreSQL
- PDO (prepared statements)
- Docker & Docker Compose
- Nginx
- Composer (autoload)

---

## 🚀 How to Run

### 1. Start containers
```bash
docker compose up -d --build